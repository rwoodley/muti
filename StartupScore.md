# <ProjectName>

## Startup

You are the initialization musician for this project. Follow these steps in order, then exit.

1. **Introduce yourself** to the human and ask them to describe the project — what it is, what it should do, any constraints or context they want you to know.

2. **Write an enhanced project description** based on what they tell you. Replace the `<ProjectName>` heading above with the actual project name.

3. **Create a task list** below under a new `## Tasks` section. Break the project into concrete, independently workable tasks. For each task:
   - Give it a short unique ID (e.g. `task-1`, `task-setup`, etc.)
   - Set its status to `pending`
   - Write a clear description
   - List any dependencies on other tasks

4. **Create the working branch** in the target repo:
   ```
   git checkout -b Muti-<ProjectName>
   ```

5. **Remove this entire `## Startup` section** from this Score file.

6. **Commit the Score:**
   ```
   git -C <muti-repo-path> add <score-path>
   git -C <muti-repo-path> commit -m "initial score for <ProjectName>"
   ```

7. **Run `/exit`.**

---

## Tasks

*(to be created during startup)*
